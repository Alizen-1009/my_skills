---
name: tdd
description: Compatibility alias for test-driven-development. Use only when another installed workflow invokes tdd or the user explicitly requests $tdd; follow the test-driven-development skill instead of defining a second TDD process.
---

# TDD Compatibility Alias

Load and follow `test-driven-development` for the complete test-driven development workflow.

This alias exists because some installed Matt Pocock workflows invoke `/tdd` directly. Do not maintain separate TDD guidance here; `test-driven-development` is the single source of truth.
